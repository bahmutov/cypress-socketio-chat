version: 2.1
orbs:
  cypress: cypress-io/cypress@3.1.4
workflows:
  build:
    jobs:
      - cypress/run:
          cypress-command: 'npm run chat:run --record --key $CYPRESS_RECORD_KEY'
          parallelism: 4
