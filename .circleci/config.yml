version: 2.1

executors:
  my-executor:
    docker:
      - image: cypress/base:14
    parallelism: 4  # Adjust the number as needed

jobs:
  cypress-tests:
    executor: my-executor
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run Cypress Tests
          command: ./run-cypress-tests.sh

workflows:
  version: 2
  build:
    jobs:
      - cypress-tests
